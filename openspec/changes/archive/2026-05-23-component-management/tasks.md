## 1. Backend — domain entities + exceptions

- [x] 1.1 Create `backend/app/domain/entities/component.py` — frozen dataclass `Component` with `id`, `mpn`, `sku`, `name`, `family`, `description`, `datasheet_url`, `location`, `supplier`, `price_per_100 (Decimal | None)`, `stock`, `tier` (Literal), `nato_score` (Literal), `country_of_origin (str | None)`, `created_at`, `updated_at`.
- [x] 1.2 Create `backend/app/domain/entities/component_purchase.py` — frozen dataclass `ComponentPurchase` with `id`, `component_id`, `purchased_at (date)`, `quantity`, `supplier`, `unit_cost`, `total_cost`, `currency`, `created_at`, `updated_at`.
- [x] 1.3 Create `backend/app/domain/repositories/component_repository.py` — Protocol with `list(filters, page, page_size)`, `get_by_id`, `get_by_mpn`, `save`, `update`, `delete`.
- [x] 1.4 Create `backend/app/domain/repositories/component_purchase_repository.py` — Protocol with `list_for_component(component_id, page, page_size)`, `save`.
- [x] 1.5 Add `ComponentNotFoundError` (404 / `COMPONENT_NOT_FOUND`) and `ComponentMpnAlreadyRegisteredError` (409 / `MPN_ALREADY_REGISTERED`) to `backend/app/core/exceptions.py`.

## 2. Backend — DB models + migration

- [x] 2.1 Create `backend/app/infrastructure/db/models/component.py` — `ComponentModel` mapped to `components` per the design. `tier` and `nato_score` declared as `String` with `CheckConstraint` containing the allowed enum values. Functional unique index on `lower(mpn)`. Per-column `lower(...)` indexes on `sku`, `name`, `family`.
- [x] 2.2 Create `backend/app/infrastructure/db/models/component_purchase.py` — `ComponentPurchaseModel` mapped to `component_purchases`. FK `component_id` with `ondelete="CASCADE"`. CHECK `quantity > 0`. Composite index `(component_id, purchased_at DESC)`.
- [x] 2.3 Register both models in `backend/app/infrastructure/db/models/__init__.py` so Alembic autogenerate sees them.
- [x] 2.4 Run `uv run alembic revision --autogenerate -m "component_management__components_and_purchases"`. Review the generated migration — replace the autogenerated raw `op.create_index(...)` calls for the `lower(...)` functional indexes with explicit `op.execute(...)` statements (Alembic autogenerate cannot express functional indexes). Implement a clean `downgrade()` that drops indexes + tables in reverse FK order.
- [x] 2.5 Verify the migration: `uv run alembic upgrade head` against the local postgres, then `uv run alembic downgrade -1`, then `upgrade head` again. All three exit 0.

## 3. Backend — repository implementations

- [x] 3.1 Implement `backend/app/infrastructure/repositories/component_repository.py` against `AsyncSession`. The `list` method builds `select(ComponentModel)` with `WHERE` clauses for the active filters; the `q` parameter generates an `OR` over `lower(...)` `ILIKE` predicates. Pagination via `LIMIT / OFFSET`. The `save` method catches `IntegrityError` on the unique `mpn` constraint and raises `ComponentMpnAlreadyRegisteredError`.
- [x] 3.2 Implement `backend/app/infrastructure/repositories/component_purchase_repository.py` with `list_for_component` ordered by `purchased_at DESC` and a paginated envelope.
- [x] 3.3 Add a paginated helper (`Page[T]` TypedDict / dataclass) in `backend/app/application/dto.py` if it does not already exist. Reused by every list endpoint going forward.

## 4. Backend — application service

- [x] 4.1 Create `backend/app/application/services/components_service.py` exposing `list`, `get`, `create`, `update`, `delete`, `list_purchases`, `enqueue_sync`. `update` ignores `mpn` / `id` / `created_at` / `updated_at` from the payload. `enqueue_sync` emits a structured log line tagged `components.sync.placeholder` with the component id.
- [x] 4.2 Add an `ComponentFilters` dataclass under the service module (the DTO the HTTP layer feeds into the service).

## 5. Backend — HTTP layer

- [x] 5.1 Create `backend/app/api/v1/schemas/components.py` with:
  - `TierLiteral = Literal["A+", "A", "B", "C", "D"]`.
  - `NatoScoreLiteral = Literal["100_otan", "otan", "allied_otan", "neutral", "high_risk", "no_otan"]`.
  - `ComponentResponse`, `ComponentCreateRequest` (required: `mpn`, `name`, `family`, `tier`, `nato_score`), `ComponentUpdateRequest` (all optional), `PaginatedComponents`, `ComponentPurchaseResponse`, `PaginatedComponentPurchases`, `ComponentSyncResponse`.
  - Validators: `stock >= 0`, `price_per_100 >= 0`, `country_of_origin` matches `^[A-Z]{2}$` when present.
- [x] 5.2 Create `backend/app/api/v1/routers/components.py` with the seven endpoints from the spec. All routes use `Depends(require_user)`. Inject `ComponentsService` via the dependency module.
- [x] 5.3 Register the new router into `backend/app/api/v1/__init__.py` (`api_v1_router.include_router(components.router)`).
- [x] 5.4 Wire `get_components_service` (+ its repository dependencies) into `backend/app/api/v1/dependencies.py`.
- [x] 5.5 Verify the OpenAPI schema picks up the new endpoints: `uv run python -c "from app.main import create_app; app=create_app(); print([r.path for r in app.routes if hasattr(r,'path')])"` includes `/api/v1/components`, `/api/v1/components/{id}`, `/api/v1/components/{id}/purchases`, `/api/v1/components/{id}/sync`.

## 6. Backend — seed script

- [x] 6.1 Create `backend/app/scripts/seed_components.py` with the `_seed(reset: bool)` async function. Inserts ten Figma-flavoured components (ACS712, B340A, BME280, ESP32-WROOM-32E, LM2596, NE555, MAX232, STM32F407VGT6, ATmega328P, plus one `no_otan` to exercise the "No OTAN" badge). For each component insert 3-6 `ComponentPurchase` rows with dates spread across the last 12 months and reasonable per-tier quantities + unit costs.
- [x] 6.2 CLI: `argparse` with `--reset` flag. Default behaviour: refuse with exit 2 if any component exists. With `--reset`: `TRUNCATE component_purchases, components RESTART IDENTITY CASCADE` first. Leave a `# TODO when Módulo lands: extend reset to truncate module-component links first` comment.

## 7. Backend — tests

- [x] 7.1 `backend/tests/unit/test_components_service.py` — happy + every error path of `ComponentsService` with mocked repos. Cover `list` filter composition (`q`, `family`, `supplier`, `tier`, `nato_score`).
- [x] 7.2 `backend/tests/integration/test_components_list.py` — `GET /` happy, empty, pagination boundaries (page 1, page 2, page beyond total → empty items), 401 without token, case-insensitive search across the four fields, `?page_size=500` → 422.
- [x] 7.3 `backend/tests/integration/test_components_create.py` — 201 happy, 409 `MPN_ALREADY_REGISTERED`, 422 invalid tier / nato_score / negative stock / missing required field.
- [x] 7.4 `backend/tests/integration/test_components_get.py` — 200 happy, 404 unknown id.
- [x] 7.5 `backend/tests/integration/test_components_patch.py` — partial update, ignored `mpn`, 404 unknown id.
- [x] 7.6 `backend/tests/integration/test_components_delete.py` — 204 happy, 204 unknown id (idempotent), cascades to purchases.
- [x] 7.7 `backend/tests/integration/test_components_purchases.py` — listing ordered DESC, pagination, empty case, 404 unknown component.
- [x] 7.8 `backend/tests/integration/test_components_sync.py` — 202 happy + log line emitted, 404 unknown component.
- [x] 7.9 `backend/tests/integration/test_seed_components.py` — happy on empty DB, refusal when components exist, `--reset` flow.
- [x] 7.10 Run `uv run pytest --cov=app --cov-fail-under=80` locally — green.

## 8. Frontend — dependencies + shared primitives

- [x] 8.1 Add `recharts` to `frontend/package.json` runtime deps. Run `pnpm install` and commit `pnpm-lock.yaml`.
- [x] 8.2 Scaffold `frontend/src/components/ui/select.tsx` — shadcn primitive on top of `@radix-ui/react-select` (already a transitive dep via `react-dropdown-menu`). If not transitive yet, add `@radix-ui/react-select` explicitly.
- [x] 8.3 Scaffold `frontend/src/components/ui/table.tsx` — minimal shadcn-style table primitives (`Table`, `TableHeader`, `TableBody`, `TableRow`, `TableHead`, `TableCell`).
- [x] 8.4 Create `frontend/src/lib/format/currency.ts` exporting `formatEuros(value: number | string | null | undefined): string` using `Intl.NumberFormat("es-ES", { style: "currency", currency: "EUR" })`. Test file with edge cases (null, 0, very small, very large).

## 9. Frontend — feature tree scaffolding

- [x] 9.1 Create the directory tree under `frontend/src/features/components/`: `api/`, `hooks/`, `pages/`, `components/`, plus `types.ts`, `schemas.ts`, `rubrics.ts`, `alerts.ts`.
- [x] 9.2 Create `frontend/src/features/components/types.ts` with `TierValue`, `NatoScoreValue`, `Component`, `ComponentPurchase`, `ComponentFilters`, `Paginated<T>`.
- [x] 9.3 Create `frontend/src/features/components/schemas.ts` with zod schemas matching the backend Pydantic shape: `componentCreateSchema`, `componentUpdateSchema`. Add validators for `mpn.min(1)`, `name.min(1)`, `tier` enum, `nato_score` enum, `stock.int().nonnegative()`, `price_per_100.nonnegative().optional()`, `country_of_origin.regex(/^[A-Z]{2}$/).optional()`.
- [x] 9.4 Create `frontend/src/features/components/rubrics.ts` with the literal Spanish copy of every Tier description and every NATO score description (the same copy that renders in `<NatoScoreHelpPopover>` and in the legend on `/components/{id}/nato`).
- [x] 9.5 Create `frontend/src/features/components/alerts.ts` with the three client-side rules: `lowStock(component) → { stock < 10 }`, `staleSupplier(purchases) → { mostRecent > 180 days old }`, `noPurchases(purchases) → { purchases.length === 0 }`.

## 10. Frontend — API + hooks

- [x] 10.1 Create `frontend/src/features/components/api/components-api.ts` mirroring `auth-api.ts`. Methods: `list(filters, page, pageSize)`, `get(id)`, `create(payload)`, `update(id, payload)`, `delete(id)`, `listPurchases(id, page, pageSize)`, `sync(id)`.
- [x] 10.2 Create `frontend/src/features/components/hooks/use-components.ts`. Accepts `{ filters, page, pageSize }`. Query key `["components", "list", { filters, page, pageSize }]`. `keepPreviousData: true` so pagination doesn't flash.
- [x] 10.3 Create `frontend/src/features/components/hooks/use-component.ts`. Query key `["components", "detail", id]`.
- [x] 10.4 Create `frontend/src/features/components/hooks/use-component-purchases.ts`. Query key `["components", "purchases", id, { page, pageSize }]`.
- [x] 10.5 Create `frontend/src/features/components/hooks/use-component-mutations.ts` exporting `useCreateComponent`, `useUpdateComponent`, `useDeleteComponent`, `useSyncComponent`. Each invalidates the right subset of query keys per the spec.

## 11. Frontend — reusable badges + popover + country select [needs-figma 47:21897, 47:17405]

- [x] 11.1 Create `frontend/src/features/components/components/TierBadge.tsx`. Pure presentational. Value → label is the value itself (`"A+"`, `"A"`, …). Colour mapping per design.md D11.
- [x] 11.2 Create `frontend/src/features/components/components/NatoScoreBadge.tsx`. Value → Spanish label per spec ("100% OTAN", "OTAN", "Aliados OTAN", "Neutral", "Alto riesgo", "No OTAN"). Colour mapping per design.md D11.
- [x] 11.3 Create `frontend/src/features/components/components/NatoScoreHelpPopover.tsx` — anchors a Popover (existing primitive) to a question-mark icon, content is the legend from `rubrics.ts`.
- [x] 11.4 Create `frontend/src/features/components/components/CountryOfOriginSelect.tsx`. Curated EU+NATO list + "Otro…" → free-text input validated by zod against ISO 3166-1 alpha-2.

## 12. Frontend — list page [needs-figma 47:15264]

- [x] 12.1 Create `frontend/src/features/components/pages/ComponentsListPage.tsx`. Top bar: title "Componentes" + the descriptive subtitle from the Figma. Toolbar: search input (debounced 300 ms, syncs `?q=`), four filter dropdowns (Familia, Supplier, Tier, NATO Score), "Sincronizar" button (per-row scope is handled inside the kebab menu), "+ Nuevo componente" primary button.
- [x] 12.2 Render the table per the columns in the spec. EUR formatted via `formatEuros`. Tier / NATO via the badge components. Row click → navigate to detail. Kebab menu per row: "Ver" (navigate to detail), "Editar" (navigate to edit), "Eliminar" (confirm dialog → DELETE mutation).
- [x] 12.3 Pagination controls at the bottom: page-of-page text + arrow buttons. Page size fixed at 25 in this US (URL param `?page=2` is supported and bookmark-friendly).
- [x] 12.4 Empty state when `total === 0`: centred "Aún no hay componentes" + "Crea el primero" CTA → `/components/new`.

## 13. Frontend — detail page [needs-figma 47:16048]

- [x] 13.1 Create `frontend/src/features/components/pages/ComponentDetailPage.tsx`. Shared `ComponentHeaderCard` component (also reused by purchase-history and NATO pages) showing MPN big, name medium, metadata grid (SKU, Familia, Ubicación, Supplier, Precio 100u, Stock, Tier badge, NATO badge, País de origen).
- [x] 13.2 Tabs strip below the header: Detalle (active by default) / Historial / Scoring OTAN. Each tab is a `<NavLink>` to the matching subroute so the tabs are deep-linkable.
- [x] 13.3 Detalle tab body: description text + recharts `<LineChart>` of stock over time (data: cumulative running stock per purchase). Right-side panel: `<ComponentAlertsPanel>` rendering the three rules from `alerts.ts`.
- [x] 13.4 Header actions: "Editar" → navigate to `/components/{id}/edit`. "Eliminar" → confirm dialog → DELETE mutation → on 204, navigate to `/components`.

## 14. Frontend — purchase history page [needs-figma 47:20273]

- [x] 14.1 Create `frontend/src/features/components/pages/ComponentPurchaseHistoryPage.tsx`. Reuses `ComponentHeaderCard` and the tabs strip with "Historial" active.
- [x] 14.2 Cost-trend `<LineChart>`: X axis = `purchased_at`, Y axis = `unit_cost`. Tooltip shows the full row (fecha, cantidad, proveedor, costo unitario, costo total).
- [x] 14.3 Below the chart: table with columns Fecha, Cantidad, Proveedor, Costo unitario, Costo total. Sorted by `purchased_at DESC`. Pagination at the bottom (page size 25).
- [x] 14.4 Empty state: chart area shows "Aún no hay compras registradas"; table shows an empty-state row with the same copy.

## 15. Frontend — NATO scoring page [needs-figma 47:21897]

- [x] 15.1 Create `frontend/src/features/components/pages/ComponentNatoScoringPage.tsx`. Reuses `ComponentHeaderCard` and the tabs strip with "Scoring OTAN" active.
- [x] 15.2 Tier breakdown panel: large `<TierBadge>` + the rubric description from `rubrics.ts`.
- [x] 15.3 NATO classification block: `<NatoScoreBadge>` + country of origin + rubric.
- [x] 15.4 Legend section at the bottom: one row per Tier value with its meaning, one row per NATO value with its meaning. Copy from `rubrics.ts`.
- [x] 15.5 `<NatoScoreHelpPopover>` anchored next to the "Scoring OTAN" heading.

## 16. Frontend — edit page [needs-figma 47:17405]

- [x] 16.1 Create `frontend/src/features/components/pages/ComponentEditPage.tsx` accepting a `mode: "create" | "edit"` prop.
- [x] 16.2 Form layout per the Figma. Fields in this exact order: MPN, SKU, Nombre, Familia, Descripción, Datasheet URL, Ubicación, Supplier, Precio (100u), Stock, Tier, NATO Score, País de origen.
- [x] 16.3 react-hook-form + zod resolver. Wire `componentCreateSchema` in create mode, `componentUpdateSchema` in edit mode.
- [x] 16.4 MPN field: editable in create mode (required), `readOnly` in edit mode (value pre-filled, NOT included in PATCH payload).
- [x] 16.5 Submit button: "Crear componente" / "Guardar cambios". Cancel button (ghost) navigates back to the source (`/components` or `/components/{id}`).
- [x] 16.6 Server-side errors: 409 (duplicate MPN) → `setError("mpn", ...)`. 422 RFC 7807 `errors[]` → per-field `setError(...)`.
- [x] 16.7 On success: create → navigate to `/components/{newId}`; edit → navigate to `/components/{id}`.

## 17. Frontend — routes

- [x] 17.1 In `frontend/src/App.tsx`, replace the five placeholder `<DashboardPlaceholder label="..." />` route elements introduced in `dashboard-shell-redesign` with the real pages:
  - `/components` → `<ComponentsListPage />`
  - `/components/new` → `<ComponentEditPage mode="create" />`
  - `/components/:id` → `<ComponentDetailPage />`
  - `/components/:id/purchases` → `<ComponentPurchaseHistoryPage />`
  - `/components/:id/nato` → `<ComponentNatoScoringPage />`
  - `/components/:id/edit` → `<ComponentEditPage mode="edit" />`
- [x] 17.2 Each protected route stays inside the existing `<RequireAuth />` wrapper. The `DashboardLayout` keeps owning the header + sidebar chrome (each page renders its body inside `DashboardLayout`).

## 18. Frontend — tests

- [x] 18.1 `frontend/src/features/components/schemas.test.ts` — zod schemas accept valid payloads and reject every documented bad input.
- [x] 18.2 `frontend/src/lib/format/currency.test.ts` — `formatEuros` happy + null / undefined / extreme values.
- [x] 18.3 `frontend/src/features/components/components/TierBadge.test.tsx` — renders the right label for every value.
- [x] 18.4 `frontend/src/features/components/components/NatoScoreBadge.test.tsx` — renders the Spanish label for every value.
- [x] 18.5 `frontend/src/features/components/hooks/use-components.test.ts` — builds the URL with the right filters / pagination from its args (MSW handler asserts the inbound URL).
- [x] 18.6 `frontend/src/features/components/pages/ComponentsListPage.test.tsx` — renders rows from a mocked feed; empty state when 0 items; typing in the search updates the URL (after the debounce); kebab-menu "Eliminar" opens the confirm dialog.
- [x] 18.7 `frontend/src/features/components/pages/ComponentEditPage.test.tsx` — create mode submits happy then navigates; 409 surfaces inline under MPN; edit mode renders MPN as read-only.
- [x] 18.8 `frontend/src/features/components/pages/ComponentDetailPage.test.tsx` — tabs render with the right one active; "Eliminar" opens dialog → confirm triggers DELETE.
- [x] 18.9 `frontend/src/features/components/components/NatoScoreHelpPopover.test.tsx` — opens on click, closes on Escape, body matches `rubrics.ts`.
- [x] 18.10 Run `VITE_ENV=development pnpm test:coverage` locally — gate ≥ 80 %.

## 19. Playwright @smoke

- [x] 19.1 Create `frontend/e2e/components.spec.ts` with two `@smoke` tests against the live docker stack (admin seeded + `seed_components` run):
  - **List → detail → historial → editar**: login, open `/components`, see ≥ 10 rows, click the ACS712 row → land on detail; click "Historial" tab → land on `/components/{id}/purchases`; see the chart and at least one table row; click "Editar" → land on `/components/{id}/edit`; edit the name; save; back on detail with the new name visible.
  - **Create new component**: from `/components`, click "+ Nuevo componente"; fill the required fields (MPN, name, family, tier, NATO score, stock); save; land on the new detail page; the metadata grid shows the filled values.
- [x] 19.2 Both tests run green against `PLAYWRIGHT_BASE_URL=http://localhost:15173`.

## 20. Documentation

- [x] 20.1 Update `ai-specs/specs/data-model.md`: move `Component` and `ComponentPurchase` from "not yet implemented" to the new column-level shape, matching the level of detail of the existing `User` entry.
- [x] 20.2 Update `ai-specs/specs/api-spec.yml`: add the `Component`, `ComponentCreate`, `ComponentUpdate`, `ComponentPurchase`, `PaginatedComponents`, `PaginatedComponentPurchases`, `ComponentSyncResponse` schemas + the seven endpoints under the `components` tag.
- [x] 20.3 Update `ai-specs/specs/development_guide.md`: add `docker compose run --rm backend python -m app.scripts.seed_components` to the "First-run" section immediately after `seed_admin`.

## 21. End-to-end verification + push + archive

- [ ] 21.1 `docker compose down -v && docker compose up -d --build` — full reset.
- [ ] 21.2 Seed admin + seed components: `docker compose run --rm backend python -m app.scripts.seed_admin --email admin@singularthings.io --password admin123` then `docker compose run --rm backend python -m app.scripts.seed_components`.
- [ ] 21.3 `curl http://localhost:18000/api/v1/components` with an access token → returns the seeded 10 rows.
- [ ] 21.4 Open `http://localhost:15173/components` in incognito → login → list visible → tour the flows manually (search, filter, detail tabs, edit, create, delete).
- [ ] 21.5 `pre-commit run --all-files` — all hooks green.
- [ ] 21.6 `git push origin main` (direct-to-main per project workflow).
- [ ] 21.7 `openspec archive component-management --yes` — commit + push the archive.
