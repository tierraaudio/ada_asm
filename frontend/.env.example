
# Application Insights Web SDK connection string (change `cloud-deployment-azure`).
# Leave empty locally → telemetry init() is a no-op. Set automatically by the
# `deploy-frontend.yml` workflow when building for prod.
VITE_APP_INSIGHTS_CONNECTION_STRING=
